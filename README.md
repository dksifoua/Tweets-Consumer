# Tweet Consumer
This program consumes tweets from Kafka.

## Dependencies
All dependencies are in the `requirements.txt` file. To install them, type this command:

`> pip install -r requirements.txt`

## Author
- Dimitri K. Sifoua